<ContentDialog x:ConnectionId='1'
    x:Class="music.Dialogs.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:music.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="登录"
    PrimaryButtonText="登录"
    SecondaryButtonText="取消"
    DefaultButton="Primary"
                                          
                                             
    Style="{StaticResource DefaultContentDialogStyle}"
    Width="400">

    <StackPanel Spacing="16">
        <!-- 登录方式选择 -->
        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
            <Button x:ConnectionId='12' x:Name="PhoneTabButton"
                    Content="手机登录"
                                                
                    Padding="16,8"
                    Style="{StaticResource AccentButtonStyle}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE717;" FontSize="14"/>
                            <TextBlock Text="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
            <Button x:ConnectionId='13' x:Name="QRTabButton"
                    Content="二维码登录"
                                             
                    Padding="16,8">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE8F1;" FontSize="14"/>
                            <TextBlock Text="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>

        <!-- 手机登录 -->
        <StackPanel x:ConnectionId='2' x:Name="PhoneLoginPanel" Spacing="12">
            <TextBox x:ConnectionId='8' x:Name="PhoneBox" 
                     PlaceholderText="手机号码"
                     InputScope="TelephoneNumber"
                     MaxLength="11"/>
            <PasswordBox x:ConnectionId='9' x:Name="PasswordBox" 
                         PlaceholderText="密码"
                         MaxLength="50"/>
            <Border x:ConnectionId='10' x:Name="PhoneErrorBorder"
                    Visibility="Collapsed"
                    Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
                    CornerRadius="4"
                    Padding="12,8">
                <TextBlock x:ConnectionId='11' x:Name="PhoneErrorText"
                           Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                           FontSize="12"
                           TextWrapping="Wrap"/>
            </Border>
        </StackPanel>

        <!-- 二维码登录 -->
        <StackPanel x:ConnectionId='3' x:Name="QRLoginPanel" Spacing="12" Visibility="Collapsed" HorizontalAlignment="Center">
            <Border Width="200" Height="200" 
                    Background="{ThemeResource ControlFillColorDefaultBrush}"
                    CornerRadius="8">
                <Image x:ConnectionId='7' x:Name="QRCodeImage" Stretch="Uniform"/>
            </Border>
            <ProgressRing x:ConnectionId='4' x:Name="QRLoadingRing" IsActive="False" Width="40" Height="40"/>
            <TextBlock x:ConnectionId='5' x:Name="QRStatusText"
                       Text="请使用网易云音乐APP扫码登录"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Button x:ConnectionId='6' x:Name="RefreshQRButton"
                    Content="刷新二维码"
                    HorizontalAlignment="Center"
                                                 
                    Visibility="Collapsed"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>

