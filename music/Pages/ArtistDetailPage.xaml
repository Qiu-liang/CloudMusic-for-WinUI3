<Page
    x:Class="music.Pages.ArtistDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:music.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Grid Margin="24,16,24,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE77B;" FontSize="20" VerticalAlignment="Center" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                <TextBlock x:Name="TitleText"
                           Text="歌手详情"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="2"
                    x:Name="PlayAllButton"
                    Click="PlayAllButton_Click"
                    Padding="12,6"
                    Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE768;" FontSize="14"/>
                    <TextBlock Text="播放全部"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- 歌手信息区域 -->
        <Grid x:Name="ArtistInfoSection" Grid.Row="1" Margin="24,0,24,16" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 歌手头像 -->
            <Border Grid.Column="0"
                    Width="160" Height="160"
                    CornerRadius="80"
                    Background="{ThemeResource ControlFillColorDefaultBrush}"
                    Margin="0,0,20,0">
                <Grid>
                    <FontIcon Glyph="&#xE77B;" FontSize="48"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Image x:Name="ArtistImage"
                           Stretch="UniformToFill"
                           Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- 歌手信息 -->
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        Spacing="8">
                <TextBlock x:Name="ArtistNameText"
                           FontSize="28"
                           FontWeight="Bold"
                           TextWrapping="Wrap"
                           MaxLines="2"
                           TextTrimming="CharacterEllipsis"/>

                <TextBlock x:Name="ArtistAliasText"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="Collapsed"/>

                <TextBlock x:Name="ArtistDescriptionText"
                           FontSize="13"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"
                           MaxLines="4"
                           TextTrimming="CharacterEllipsis"
                           Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- 加载中 -->
        <Grid Grid.Row="1" x:Name="LoadingPanel">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                <ProgressRing IsActive="True" Width="32" Height="32"/>
                <TextBlock Text="加载中..." 
                           Style="{StaticResource BodyTextBlockStyle}"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Grid>

        <!-- 分页导航 -->
        <NavigationView Grid.Row="2"
                        x:Name="ArtistNavView"
                        PaneDisplayMode="Top"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        SelectionChanged="ArtistNavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="热门歌曲" Tag="songs" IsSelected="True"/>
                <NavigationViewItem Content="专辑" Tag="albums"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Page>
